# Núcleo do lakehouse (obrigatório) — local, gratuito, sem Spark/JVM
duckdb>=1.5           # motor SQL in-process
deltalake>=1.6        # formato de tabela Delta: ACID + time-travel (delta-rs)
pyarrow>=15           # colunar (Parquet) por baixo do Delta
pandas>=2.0
scikit-learn>=1.3     # ML de risco (já usado no estudo)
dbt-duckdb>=1.9       # silver/gold como modelos SQL (lineage + testes + docs); usa o plugin delta
statsmodels>=0.14   # modelos mistos (negativas por tipo de dia)
xgboost>=2.0        # comparação de modelos preditivos
lightgbm>=4.0
pingouin>=0.5        # ICC de confiabilidade

# Ingestão IoT / tempo real (opcional) — para ingestion_api.py
# fastapi>=0.110
# uvicorn>=0.29

# Crescer mais (opcional):
# dagster>=1.6         # orquestração por assets, agendamento e linhagem visual
# mlflow>=2.10         # registro/versão de modelos e experimentos
